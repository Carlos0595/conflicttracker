# conflict-tracker

Proyecto Spring Boot (Java) para gestionar conflictos, países y eventos. Usa H2 en memoria y Spring Data JPA.

## Requisitos
- Java 25
- Maven
- IntelliJ IDEA (opcional)
- Git (opcional)

## Ejecutar localmente
Desde la raíz del proyecto:

1. Compilar:
```bash
mvn clean package
